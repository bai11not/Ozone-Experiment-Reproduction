# 论文结果资产说明

这里放的是当前已有论文表格的副本，学生提交结果时需要向这些字段对齐。

关键文件：

- `table1_main_raw_comparison.csv`：主模型和 baseline 原始对比表；
- `table2_ablation.csv`：消融实验表；
- `table3_pe_stratified_summary.csv`：PE 分层统计表。

学生新结果不要直接覆盖这些文件。应先写入自己的 `results.csv`，由结果整理组统一合并。
